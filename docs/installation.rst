============
Installation
============

At the command line::

    $ easy_install pyfuzzy_toolbox

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyfuzzy_toolbox
    $ pip install pyfuzzy_toolbox
